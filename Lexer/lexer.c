#include <regex.h>
#include <stdio.h>



char* regex_plus = "[+]";
char* regex_minus = "[-]";
char* regex_number = "[:number]";
char* regex_lparen = "[:(]";
char* regex_rparen = "[:)]";
char* regex_multiply = "[:*]";
char* regex_divide = "[:/]";
char* regex_equals = "[:=]";
char* regex_if = "[:if]";
char* regex_else = "[:else]";